# 待实现
## 平台改名
MarkItDown 批量转换 -》文本信息提取与转换平台（转换为md）

## 文件批量处理
新增文件批量下载；
增加删除功能：可批量删除文件或单独删除；
端口都从.env中配置：且区分INNER_PORT.OUTER_PORT;
上传时区分任务：一般都上传到默认任务，也可上传到新增的任务，同一任务中的文件可批量下载或删除。也可以一键清楚全部任务。但默认任务即使文件删除了，任务名也要保留；
任务名可输入可选取、按钮始终可见、DropZone恢复点击、内外端口分离;
```
任务隔离：所有转换后的文件存储在 converted/{任务名}/ 下，文件名附加了随机 ID 以防同名文件冲突。
批量下载：点击“批量下载(ZIP)”，后端会即时扫描对应文件夹并打包成压缩包流式输出，不占用额外磁盘空间。
批量/单项删除：
单项：针对某个具体的 .md 文件。
批量（清空任务）：删除任务文件夹。代码逻辑保证了 default 文件夹被删后会立即重建，满足“默认任务保留名”的要求。
一键清理：递归删除 converted 下所有内容。
端口配置：
.env 里的 OUTER_PORT 是你在浏览器访问的端口（如 8888）。
INNER_PORT 是容器内部 Uvicorn 监听的端口。
前端交互：增加了上传中的视觉反馈，并能够区分文件上传和文件夹上传。
```

# 已实现
